# main.py dependencies
toml==0.10.2
numpy==1.21.2
pandas==1.3.3
openpyxl==3.1.2
reportlab==4.0.5
inflect==5.3.0
PyPDF2==3.0.1

# requests==2.26.0

# for GUI 
# tk==8.6.10
# tkinter # no version 
# tkinter is a part of the Python standard library and doesn't need to be installed separately using pip. 

# data_consolidation.py dependencies
# (No need to list toml and pandas again since they are already listed above)

# utilities.py dependencies
# (No need to list toml and pandas again since they are already listed above)

# file_processing.py dependencies
# (No need to list os & re & json (build in), re, pandas, toml, json again since they are already listed above)
